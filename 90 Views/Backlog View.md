# Backlog View

sort_direction:: asc

This view reads all painpoints from `02 Problem Analysis/01 Painpoints` and summarizes task assignment plus linked solutions. Use the inline sort control above instead of top-level Properties.

```dataviewjs
await dv.view("00 System/views/backlog");
```

## Quick Controls

- `sort_direction`: `asc`, `desc`
