views:
  - type: table
    name: Table
    filters:
      and:
        - painpoint_cluster.isEmpty()
        - entity_type == "painpoint"
    order:
      - file.name
    sort:
      - property: file.name
        direction: ASC
